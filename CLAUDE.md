# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently **empty** — it contains only a README.md with the project title (`Y.S-`) and no source code, build configuration, tests, or tooling.

There is no language, framework, build system, test runner, or package manager established yet. Do not assume any stack; wait for the user to introduce code or describe their intent before scaffolding.

When the repository gains real content, this file should be updated with:
- Build, lint, test, and run commands (including how to run a single test)
- High-level architecture that spans multiple files
- Project-specific conventions that aren't obvious from the code

## Git Workflow

- Active development branch for Claude-authored changes: `claude/add-claude-documentation-5veug`
- Default branch: `main`
- Develop, commit, and push to the designated feature branch; do not push directly to `main` without explicit permission.
